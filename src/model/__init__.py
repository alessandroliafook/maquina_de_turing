class Configuration(object):

    def __init__(self, state, tape, steps, position):       
        self.state = state
        self.tape = tape
        self.steps = steps
        self.position = position
